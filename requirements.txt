# Fijado y verificado contra wheels linux/arm64 en Fase 0 (ver scripts/arm64_audit.md).
# torch se instala aparte, desde el índice CPU dedicado (ver Dockerfile) — no vía este archivo,
# porque requiere --index-url distinto al de PyPI.

polars==1.43.0
deltalake==1.6.2
pyarrow==25.0.0
pydantic==2.13.4
sentence-transformers==5.6.1
faiss-cpu==1.14.3
usearch==2.26.0        # sustituto de hnswlib (sin wheels arm64, decisión Fase 0)

httpx==0.28.1
pytest==8.4.2
